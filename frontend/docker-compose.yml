version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nextjs-dashboard
    ports:
      - "3001:3000"
    restart: always
    environment:
      - NODE_ENV=production
