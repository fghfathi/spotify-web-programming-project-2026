# The local virtualenv is built for the host's Python and must never be copied.
.venv/
venv/

# Local SQLite database — the container uses Postgres.
db.sqlite3
*.sqlite3

# Local secrets. Compose supplies the container's environment.
.env

__pycache__/
*.py[cod]
staticfiles/

.git/
.gitignore
.DS_Store
